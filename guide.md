b1: chạy lệnh:

```zsh
/Library/PostgreSQL/18/scripts/runpsql.sh
```

b2: nhập mật khẩu của user postgres khi được yêu cầu.

b3: lệnh tạo database

```sql
create database new_database;
```

xóa database (ngắt kết nối trước khi xóa)

```sql 
drop database new_database;
```

check = \l

quit = \q

Nhớ có dấu chấm phẩy